{
  "name": "@twcrm/ui",
  "version": "0.1.0",
  "private": true,
  "description": "全局设计系统 — 主题/颜色/Tailwind 配置/共享组件",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.5.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "rimraf": "^6.1.3"
  }
}
