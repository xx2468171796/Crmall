{
  "name": "enterprise-nexus",
  "version": "0.1.0",
  "private": true,
  "packageManager": "yarn@4.6.0",
  "workspaces": [
    "apps/portal",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "db:generate": "turbo db:generate",
    "db:push": "turbo db:push"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  }
}
