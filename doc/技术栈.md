# ⚡️ 技术架构与选型方案 (2026 Ultimate Edition)

本文档定义了系统开发的最高技术标准。
架构核心采用 **Next.js 16 (Turbopack)**  全栈策略，由 **Windsurf AI** 驱动开发，使用 **Yarn v4** 管理依赖，并基于 **Rust (Tauri v2)**  实现全平台交付。

---

## 0. AI 辅助开发环境 (AI-Native Workflow)

> **核心引擎**：利用最新的 AI IDE 与 Agent 模式，极大提升 UI/UX 构建效率。

| 模块 | 技术选型 | 配置/版本    | 核心说明                                      |
| :----- | :--------- | :------------- | :---------------------------------------------- |
| **IDE**     | **Windsurf**         | Latest       | 下一代 AI 编辑器，开启 `Flow` 模式深度理解上下文    |
| **AI Agent**     | **UI-UX-Pro-Max**         | Custom Mode  | **核心开发模式**。专注于生成高保真、极致交互的现代化 UI 代码  |
| **包管理器**     | **Yarn**         | **v4 (Berry)**             | 极速依赖管理，启用 `node-modules` 兼容模式                  |
| **代码规范**     | **Biome**         | Latest       | 替代 ESLint/Prettier，基于 Rust，速度快 35 倍 |
| **Git 规范**     | **Husky**         | + Commitlint | 提交信息标准化拦截                            |

---

## 1. 前端技术栈 (Frontend)

> **版本策略**：Next.js 16 激进版本，全面启用 React 19 新特性与 Turbopack 构建引擎。

| 模块 | 技术选型 | 版本/特性 | 核心说明                                             |
| :----- | :--------- | :---------- | :----------------------------------------------------- |
| **应用框架**     | **Next.js**         | **v16**          | 默认启用 **Turbopack**，毫秒级 HMR，深度集成 React Compiler       |
| **构建引擎**     | **Turbopack**         | **Built-in**          | **Rust 编写**。替代 Webpack，开发体验零延迟                       |
| **核心库**     | **React**         | **v19**          | 稳定使用 Server Actions, `useOptimistic`, `use` Hook                     |
| **语言**     | **TypeScript**         | **v5.x**          | 严格模式 (Strict Mode)，全链路类型安全               |
| **样式引擎**     | **Tailwind CSS**         | **v4.0**          | **Oxide Engine** 驱动，零配置，构建速度提升 10x                      |
| **UI 主系统**     | **shadcn/ui**         | Latest    | 基于 Radix UI，源码级引入，拥有最大定制自由度        |
| **UI 辅助库**     | **HeroUI**         | v2+       | (原 NextUI) 补充高颜值交互组件，风格与 Tailwind 统一 |
| **图标体系**     | **Lucide React**         | Latest    | 统一的现代化 SVG 图标库                              |
| **状态管理**     | **Zustand**         | v5        | 极简的全局状态管理，替代 Context/Redux               |
| **数据请求**     | **TanStack Query**         | v5        | 服务端状态同步、智能缓存、乐观更新策略               |
| **表单校验**     | **React Hook Form**         | v7+       | 结合 **Zod** 实现高性能表单与 Schema 验证                   |

---

## 2. 后端与数据栈 (Backend & Data)

> **版本策略**：Serverless 思维优先，去 API 化设计，数据中台与业务分离。

| 模块 | 技术选型 | 版本/备注  | 核心说明                                           |
| :----- | :--------- | :----------- | :--------------------------------------------------- |
| **后端逻辑**     | **Server Actions**         | Next.js 16 | **去 API 化**。直接在服务端组件调用数据库，类型自动推导         |
| **ORM 框架**     | **Prisma**         | **v6+**           | 支持 Edge Runtime，完美的 TypeScript 自动生成      |
| **数据库**     | **PostgreSQL**         | **v17**           | 生产环境首选，利用 JSONB 存储复杂配置数据          |
| **身份认证**     | **Auth.js**         | **v5**           | (原 NextAuth) 完美适配 App Router 的服务端认证方案 |
| **Headless CMS**     | **Directus**         | Latest     | **数据中台**。直连 PostgreSQL，提供可视化数据管理 API          |
| **对象存储**     | **MinIO**         | Latest     | **S3 兼容**。私有化部署的文件存储服务，Docker 部署            |
| **数据校验**     | **Zod**         | v3.x       | 前后端共用 Schema，运行时严格校验                  |

---

## 3. 基础设施 (Infrastructure)

> **版本策略**：以 Docker 为核心，追求极致的轻量化与可迁移性。

| 模块 | 技术选型 | 版本/备注 | 核心说明                                           |
| :----- | :--------- | :---------- | :--------------------------------------------------- |
| **操作系统**     | **Debian**         | **12 (Bookworm)**          | 稳定、安全、占用极低的 Linux 发行版                |
| **运行环境**     | **Node.js**         | **v24 (Current)**          | 配合 Next.js 16 发挥最佳性能                       |
| **容器编排**     | **Docker Compose**         | v2+       | 定义服务依赖（App + DB + Redis + MinIO），一键拉起 |
| **运维面板**     | **1Panel**         | Latest    | 现代化可视化运维面板，替代宝塔，管理容器更专业     |
| **网关/反代**     | **OpenResty**         | Latest    | 基于 Nginx 的高性能流量入口，支持 Lua 扩展         |
| **部署模式**     | **Standalone**         | -         | Next.js 独立构建模式，生成极小 Docker 镜像         |

---

## 4. 桌面端跨平台 (Desktop / Cross-Platform)

> **版本策略**：Tauri v2 正式版，Rust 桥接层。

| 模块 | 技术选型 | 版本/备注    | 核心说明                                           |
| :----- | :--------- | :------------- | :--------------------------------------------------- |
| **构建框架**     | **Tauri**         | **v2.0**             | Rust 驱动，支持跨平台（Win/Mac/Linux/Android/iOS） |
| **前端复用**     | **Next.js Export**         | SSG          | 将 Web 前端构建为静态资源注入 Tauri WebView        |
| **本地数据库**     | **SQLite**         | v3           | 配合 Tauri SQL 插件，实现桌面端离线数据存储        |
| **系统通信**     | **Rust**         | 2024 Edition | 编写高性能后台插件，处理文件、系统托盘等原生能力   |

---

## 5. 开发工具链 (DevTools)

- **包管理器**: `Yarn` (v4.0+ Berry, node-modules mode)
- **API 调试**: `Bruno` / `Postman`
- **编辑器**: `Windsurf` (AI-First IDE)
- **数据库客户端**: `TablePlus` 或 `Prisma Studio`

---

## 6. 核心技能矩阵 (Skill Matrix)

> **说明**：驾驭本技术栈所需的关键技能点与概念。

### 🎨 前端与交互 (Frontend & UX)

| 技能领域 | 关键知识点 (Keywords) | 应用场景                                |
| :--------- | :---------------------- | :---------------------------------------- |
| **Modern React**         | `Server Components (RSC)`, `Suspense`, `useOptimistic`, `Actions`                | 构建无白屏、极速响应的用户界面          |
| **Next.js Arch**         | `App Router`, `Layouts`, `Streaming`, `Middleware`                | 掌握 Next.js 16 的路由与渲染机制        |
| **Atomic CSS**         | `Tailwind v4`, `Variables`, `Dark Mode`, `Shadcn Theming`                | 快速还原 UI-UX-Pro-Max 设计稿，定制主题 |
| **State Mgmt**         | `Zustand Store`, `Server State (TanStack Query)`                    | 分离 UI 状态与服务端数据缓存            |
| **Validation**         | `Zod Schema`, `Type Inference`                    | 表单验证与前后端类型自动对齐            |

### ⚙️ 后端与架构 (Backend & Architecture)

| 技能领域 | 关键知识点 (Keywords) | 应用场景                         |
| :--------- | :---------------------- | :--------------------------------- |
| **TypeScript**         | `Generics`, `Utility Types`, `Type Narrowing`                  | 全链路类型安全，杜绝 `any`            |
| **Serverless**         | `Server Actions`, `Edge Runtime`, `Route Handlers`                  | 编写无需传统 API 层的业务逻辑    |
| **Database**         | `Prisma Schema`, `Relations`, `Migrations`, `JSONB`                | 数据库建模与结构变更管理         |
| **Auth**         | `NextAuth v5`, `OAuth Flow`, `Session Management`                  | 处理用户登录、权限控制与路由保护 |

### 🦀 系统与底层 (System & Rust)

| 技能领域 | 关键知识点 (Keywords) | 应用场景                             |
| :--------- | :---------------------- | :------------------------------------- |
| **Rust Basics**         | `Ownership`, `Borrow Checker`, `Structs`, `Pattern Matching`                | 编写 Tauri 后端插件，保证内存安全    |
| **Tauri IPC**         | `Commands`, `Events`, `Window Management`, `System Tray`                | 前端(JS)与系统底层(Rust)的高性能通信 |
| **Native API**         | `File System`, `Shell`, `OS Integration`                  | 调用操作系统原生能力（如文件读写）   |

### 🤖 AI 协作 (AI Workflow)

| 技能领域 | 关键知识点 (Keywords) | 应用场景                               |
| :--------- | :---------------------- | :--------------------------------------- |
| **Prompt Eng**         | `Context Management`, `Chain of Thought`, `System Prompts`                  | 引导 Windsurf 生成高质量代码           |
| **AI Debugging**         | `Error Context`, `Refactoring Prompts`                    | 快速定位并修复 Turbopack 或类型错误    |
| **UI Gen**         | `Screenshot to Code`, `Component Refinement`                    | 利用 AI 快速将设计图转化为 Shadcn 组件 |

---

## 💡 环境配置指南 (Quick Start)

### 1. 包管理器初始化 (Yarn v4)

本项目强制使用 Yarn Berry。

```bash
corepack enable
yarn set version berry
# 确保 .yarnrc.yml 中配置了 nodeLinker: node-modules
```